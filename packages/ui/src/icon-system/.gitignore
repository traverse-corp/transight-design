# 빈 파일 — 이전엔 sprite.gen.tsx / icons.gen.ts를 ignore했으나
# shadcn registry가 GitHub raw fetch로 install 파일을 가져오므로 commit 필요.
# 두 gen 파일은 svg/ 변경 시 `npm run build:icons`로 재생성 후 함께 commit한다.
# (수동 수정 금지 — 빌드가 덮어씀)
