# apps/registry — placeholder

Phase 4~6에서 Next.js로 구성한다. 역할:

- 문서 사이트 (컴포넌트별 라이브 프리뷰, props 표, 코드 복사)
- 빌드된 `public/r/*.json` 정적 호스팅 (`npx shadcn add @transight-design/...` 진입점)
- (옵션) MCP 서버 연결로 AI 에이전트가 직접 설치 가능하게

현재는 디렉토리만 잡아두고 실제 셋업은 Phase 4 진입 시 진행한다.
