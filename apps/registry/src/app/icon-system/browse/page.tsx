import { PreviewShell } from '../_components/preview-shell'

export const metadata = {
  title: 'Icon System · Browse — Transight Design'
}

const IconSystemBrowsePage = () => <PreviewShell />

export default IconSystemBrowsePage
